import React from "react";

const Header = () => {
  return (
    <div>
      <div>
        <button className="btn btn-success active">로그인</button>
        <button className="btn btn-secondary active">회원가입</button>
      </div>
    </div>
  );
};

export default Header;
